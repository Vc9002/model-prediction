# Handoff — 2026-08-26 scan-resolution session

Branch `research/mlb-v9`. Eight commits, d1dffb7 → 2fee0ac (on bfed6d6),
plus the handoff-doc commit itself (9 on the branch now, all unpushed).
Working tree clean at handoff. Full detail in `docs/DEBUG.md` 2026-08-26
(morning scan + evening resolutions + night continuation) — this file is
the short map.

## Done (all committed)

1. **13 dead v9 features root-caused and fixed** (d8393d3) — four lookup
   bugs: nested snapshot schema in `batter_priors`, team-name vs team-ID
   keys in `bullpen_state`, wrong feature name in `mlb_v9_features`,
   missing starter crosswalk in the table builder. Rebuilt v3 table
   (6,678 rows, hash 6b079f58cd82) **passes all distribution-audit gates**;
   3 pairs documented construction-collinear (`KNOWN_COLLINEAR_PAIRS`).
   Regression tests revert-verified.
2. **v3 ablation ladder re-run** (d8393d3) — evaluator gained
   `--parquet`/`--manifest` + `v3_*` feature sets; contract
   `manifests/mlb_v9_feature_table_v3_eval.json` + `*_event_ids_v3.json`
   cohorts. 2,000-bootstrap results in
   `outputs/research/mlb_evaluator/report_v3_20260826.json`:
   **v3_starters KEEP** (dLL=-0.00208, P=0.93), **v3_full KEEP**
   (dLL=-0.00300, P=0.91, AUC 0.5743), offense borderline (P=0.89),
   bullpen/platoon no signal. First real v9 signal — the pre-fix ladder
   measured all-null on these families.
3. **Artifact hash "mismatches"** (f23ca1e) — both proven deliberate
   never-re-signed cases; verifier documents `KNOWN_MISMATCH_ARTIFACTS`
   instead of reporting gaps. Artifacts untouched.
4. **Orphaned configs** (f23ca1e) — nfl/wnba `-temperature` configs
   removed; `mlb-v9-candidate-1.json` kept (quarantine is load-bearing).
5. **Ledger settlement backlog + 6 identity conflicts** (724d84a +
   earlier audit work) — 22-row cs2 backlog + 27 unlocked settlements
   applied; `scripts/resolve_ledger_conflicts.py` resolves conflicts from
   canonical SQLite (latest-settlement authority, lineage reference,
   stale-survivor archive, payload sync); audit signature stake-normalized
   (4dp). Final audit: **0 repairs / 0 conflicts / 0 anomalies,
   integrity ok**. Four payloads accidentally replaced by an early
   resolution run were restored from the daily backup
   (`model-prediction-runtime/backups/ledgers.20260826T072220Z.db`) as
   audited update events.
6. **launchd** (a28bcc8) — daily plist byte-synced to `ops/launchd/` and
   reloaded; `production` + `rebuild-shadow` jobs were **disabled** in
   launchd (37h undocumented stall) — operator approved re-enable; both
   loaded, fired, completed exit 0. Health no longer reports the stale
   canary.
7. **Soccer results source replaced** (ea1b31a) — API-Football v3 client
   (`data_sources/api_football.py`, fail-closed without
   `API_FOOTBALL_KEY`, provenance snapshots) wired into daily step1b +
   `cmd_collect_scores`; dormant Odds API path kept as documented
   fallback; source policy updated.
8. **NRFI model improved** (ef5b017) — 2x league run-rate constant fixed
   (0.52 per-team mean → 1.036 per-game total, pinned by test); new
   research module `models/mlb_first_inning.py` (PIT first-inning ledger +
   fitted LR). Holdout 1,337 games: logloss 0.6910 vs incumbent 0.6945 vs
   market proxy 0.6950; calibration error 0.0163 vs proxy 0.0309.
9. **Docs** (d1dffb7) — DEBUG/PROJECT_STATUS/CHECKLIST 2026-08-26
   sections; `docs/RESEARCH_BACKLOG.md` re-deleted (unique content ported
   to ROADMAP.md); stale references retargeted; README launchd cadence
   corrected; `tmp/` gitignored.
10. **Data-gap audit** — full per-sport/feature/model scan; findings +
    corrected false alarms recorded in DEBUG.md (see below for open items).

## Not finished

- **API-Football activation** — no key provisioned. Operator: register at
  api-football.com (free ~100 req/day), set `API_FOOTBALL_KEY` in the
  daily job env, then live-verify: 16 of 20 league IDs in
  `API_FOOTBALL_LEAGUE_IDS` are doc-derived ("verify via /leagues" in the
  comment), AET/PEN goal shape, per-minute pacing.
- **Real Polymarket NRFI quote capture** (the "true market edge" step) —
  the repo stores no real NRFI market prices (`sportsbook="model_fair"`).
  Capturing live NRFI BBOs from the existing Polymarket snapshot flow and
  wiring them into mlb-nrfi-v1 rows is the prerequisite for measuring real
  CLV. New model research script:
  `scripts/mlb_nrfi_first_inning_research.py`
  (`tmp/nrfi-first-inning-research.json`).
- **Public-apis research agent** was still running at handoff — its
  report (API-Football alternatives incl. football-data.co.uk check, and
  broader dataset candidates) lands after this handoff.
- **WNBA availability snapshots** stale 1.5 days — today's WNBA calls
  degraded to `NO_CALL_AVAILABILITY_UNAVAILABLE`; capture step needs
  investigation.
- **Statcast aggregate parquets** 3 days behind — manual-only
  `scripts/ingest_statcast_aggregates.py`, not wired into daily (no
  serving impact; provenance/manifest inputs stale).
- **Market-snapshot lineage** absent for 7,263 esports/soccer/KBO/NPB
  ledger rows (research rows created with prices but no snapshot linkage).
- **NBA/NFL odds** — zero sources wired; TheRundown shortlisted as the
  candidate second-book/odds source before seasons start.
- **v9 follow-ups** — platoon/bullpen construction-collinear pairs need a
  pitch-level or roster-role source before they can diverge; v3 ladder
  results are the new baseline for challenger decisions (promotion
  governance per ROADMAP.md unchanged).

## Notes

- No commits were pushed — local branch only (`research/mlb-v9`,
  8 commits ahead of bfed6d6).
- Ledger evidence: sqlite backup
  `model-prediction-runtime/ledgers/ledgers.bak-conflicts-20260826T193311Z.db`
  + per-workbook `.bak-pnl-sync-*` copies under `data/model_ledgers/`.
- The concurrent WNBA-total artifact-identity repair session merged cleanly
  (wnba-total-margin rename rides in f23ca1e).
- Memory files updated: v9 research direction (pre-08-26 nulls were
  measured on dead features), tennis coverage (Polymarket has ATP/ITF),
  rebuild-isolation (scheduler re-enabled), project status.
