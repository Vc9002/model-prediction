# Handoff — post-burn-in research execution, session 2026-08-17 (afternoon)

**Branch:** `research/mlb-v8-reproduction` (worktree `/Users/vincentc9002/worktrees/mlb-research`)
**State:** all session work committed except the final calibration-wiring batch (see "Uncommitted" below); 1890 tests green as of the last full run; ruff clean.

## The one actionable improvement found

**WNBA v4 + temperature calibration (T=0.8) beats WNBA v4 identity on the locked holdout.**

- Holdout (n=163, v4's own recorded split): Brier 0.214138 → 0.212096 (**Δ = −0.002042, first challenger of the entire session to clear the −0.002 bar**), LogLoss 0.618737 → 0.613928, ECE 0.0465 → 0.0348, calibration slope 1.383 → 1.106.
- Mechanism: v4 is underconfident (slope 1.38). T=0.8 sharpens. Fitted on the 143-game validation window only.
- Bootstrap P(better)=0.806; CI wide because n=163. This is the small-sample caveat — the settled-picks test below is the confirmation mechanism.
- Registry: `wnba-v4-temperature-calibration-T0.8` (completed). The earlier entry `wnba-v4-temperature-calibration` is VOID (it misstated T=1.1).

## Wired so far (the freeze)

1. `src/model_prediction/calibration.py` — new `TemperatureCalibrator` class (grid 0.35–3.0, log-loss fit, identity fallback <100 samples, metadata + hash like Platt/Isotonic). Convention documented: **T<1 sharpens, T>1 softens**.
2. `src/model_prediction/models/learned_market.py` — `probability()` now reads an optional `calibration` block on the moneyline model: `{"method": "temperature", "temperature": T}`. Artifacts without the block follow the legacy path bit-for-bit. Unknown method / T≤0 fail closed (ValueError).
3. Tests: 3 new in `tests/test_calibrators.py`, 3 new in `tests/test_learned_market.py` (10/10 and 6/6 pass respectively).

## Next steps, in order (the remaining shadow chain for WNBA temperature)

1. **Settled-picks validation (explicit operator ask).** Pull WNBA settled rows from the runtime-root SQLite ledger (`ledgers/ledgers.db`, table `ledger_records`; read-only, do NOT mutate), take each settled row's recorded model probability (v4 identity), apply the T=0.8 transform, and compare Brier/LogLoss/ECE of raw vs transformed on the settled outcomes. This tests the calibration on the decisions the system actually made.
2. **Create the frozen candidate artifact** `config/models/wnba-elo-trend-lr-v4-temperature.json`: copy v4's raw payload, add the calibration block, `model_version` → `wnba-elo-trend-lr-v4-temperature`, recompute `artifact_hash` via `learned_market.artifact_hash()` (canonical = payload minus artifact_hash, sorted-keys sha256). Verify it loads (`LearnedMarketArtifact` validates hash on load).
3. **Prospective shadow** on the flat ledger vs live quotes (zero real units); pre-register N≥30 settled picks BEFORE the shadow starts.
4. **Settled comparison → `PROMOTION_CANDIDATE` → operator decides.** Promotion to the Main ledger is the operator's gated call — do not promote without it. WNBA moneyline produces real `QUALIFIED_SHADOW_CALL` rows, so any serving change is high-stakes.

## MLB moneyline: v8 confirmed at its ceiling (no v9 promotion)

- Steps 3–5 fully executed: 14-variant ablation matrix on two data windows (zero KEEP; C/D/I/J REJECT on the clean window), LR vs XGBoost (LR wins, P(XGB better)=0.08), calibration challengers (Temperature best at Δ−0.0002, noise), trend-removal on v8's own split (Δ−0.00006, noise).
- Step 7 distribution family closed: gamma_poisson beats NB, independent_poisson, ZINB (P(better) 0.175 / 0.049 / 0.0 on ~3k games).
- Shadow features built, not promoted: `features/air_density_weather.py` (temperature deviation; totals Δ−0.0007 INCONCLUSIVE), starter-IP explicit split (null).
- Blocked on data (recorded in registry, do not fake): PIT weather (L), projected/confirmed lineup (M/N), time-through-order (no PA-level data).
- The model already beats the market's no-vig totals line on the 171-game 2026-07 window (0.238 vs 0.242) — baseline for the future totals rebuild.

## Where the numbers and scripts live

| Thing | Location |
|---|---|
| Ablation matrix harness | `scripts/mlb_v9_ablation_matrix.py` (+ `--min-date`) |
| Distribution backtest | `scripts/mlb_v9_distribution_backtest.py` |
| Air-density backtests | `scripts/mlb_v9_air_density_backtest.py`, `..._totals.py` |
| Starter-IP test | `scripts/mlb_v9_starter_ip_backtest.py` |
| ZINB test | `scripts/mlb_v9_zinb_test.py` |
| Calibration + XGB | `scripts/mlb_v9_calibration_xgb.py` |
| Trend removal on v8 split | `scripts/mlb_v9_trend_removal.py` |
| WNBA v5 paired test | `scripts/wnba_v5_paired_test.py` |
| Frozen feature table | `outputs/research/mlb_v9_ablation/pit_mlb_v9.jsonl` (+manifest) |
| All results | `outputs/research/mlb_v9_*/`, `outputs/research/wnba_v5/` |
| Experiment registry | worktree-local `data/runs.db` (`python -m model_prediction.experiment_registry list/show`) |

## Uncommitted (commit first thing next session)

- `src/model_prediction/calibration.py` (TemperatureCalibrator)
- `src/model_prediction/models/learned_market.py` (calibration dispatch)
- `tests/test_calibrators.py`, `tests/test_learned_market.py` (new tests)
- `scripts/mlb_v9_calibration_xgb.py`, `scripts/mlb_v9_trend_removal.py`, `scripts/wnba_v5_paired_test.py`
- `docs/HANDOFF_2026-08-17.md`

Run `env PYTHONPATH=src:. ~/model-prediction/.venv/bin/python -m pytest tests/ -q` and ruff before committing.

## Remaining prompt items (untouched, in plan order)

- Step 7 tail: joint ML/RL/totals rebuild; log-score stacking (needs the rebuild first).
- Step 8: WNBA v5 (paired test ran — null; v5 feature challenger is dead, temperature calibration is the WNBA win instead) → WNBA pace×ORtg → NFL calibration challengers (test same temperature treatment; check NFL slope) → tennis v2 surface-weighting → soccer league split + Dixon-Coles → esports Glicko → KBO/NPB starters.
- Calendar-bound: ≥30 settled shadow picks before any promotion decision.

## Key lessons logged (task-observer log)

- Observation 47: never double-background (`&` inside a run_in_background command reports false completion).
- Observation 48: verify merge-commit parents (`git merge-base --is-ancestor`) before trusting "synced from main" claims — this session's worktree was 11 commits stale vs its claim.
- Observation 49: Python `list > list` is lexicographic, not elementwise — use `np.asarray` before comparing score vectors; a fake Brier improvement (Δ −0.0185) was caught only by a physics plausibility check.
