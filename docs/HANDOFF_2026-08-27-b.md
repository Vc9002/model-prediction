# Handoff — 2026-08-27 (second session) — wire spread/total/NRFI into production

Repo clean at commit `a82b5b3` (governance: close Phase-23 promotion-gate
acceptance checklist gaps — pushed). Nothing from this handoff's task has
been started; no files touched, no commits made. This ran out of context
before any edits, purely from investigation — pick up straight from here.

## Operator directive (explicit, given across several turns — do not re-ask)

"All sports should have ml, spread, and total in production, wire run
first inning into production as well." Then, when I raised that WNBA
total has proven negative ROI evidence and that spread/total don't exist
for most sports: **"im not buying so does it matter"** and **"override
it"** — i.e. the operator has explicitly waived the evidence-quality
objection for the WNBA-total case, and confirmed this is shadow-ledger
work (not real-money order execution), so the normal caution about
serving a model with negative-ROI evidence does not apply. Do not
re-litigate that point with the operator; proceed.

**Scope is still real**: "wire all sports' spread/total" is NOT
achievable as a config change — 9 of 13 sports (NBA, NFL, Soccer, Tennis,
CS2, Dota2, LoL, Valorant, R6, KBO, NPB) have **no spread/total model
built at all**, only moneyline. Building those from scratch is a separate
research project per sport (data, features, walk-forward validation) —
out of scope for "wiring." The operator has not been told this needs to
be deferred yet in so many words this session, but the prior session did
lay it out and the operator's replies ("not buying", "override it") were
specifically about the two sports (MLB, WNBA) that already have
artifacts — treat the 9-sport buildout as a separate future ask unless
the operator says otherwise when this resumes.

## What's actually in scope for THIS handoff (2 tasks)

### Task 1 — wire 4 existing-but-unservable artifacts into production

Four artifacts exist on disk but have `market_models: {}` (empty JSON),
so `ProductionModelRegistry.load()` can't serve them (confirmed by
direct inspection this session):

- `config/models/measured-edge-totals-v3.json` — MLB total. Has
  `offset`, `scale`, `calibration_method`, `calibration_windows`,
  `base_score_model_version`, `trained_on_games`. No `sport` field, no
  `market_models` block.
- `config/models/measured-edge-margin-v3.json` — MLB spread. Same shape.
- `config/models/wnba-spread-margin-v1.json` — WNBA spread. Has
  `expected_margin_formula`, `cdf_family`, `prediction_formula`,
  `margin_sd: 10.5`, `qualification: {qualified: false, status:
  "research_baseline", note: "backtest pending"}`.
- `config/models/wnba-total-margin-v1.json` — WNBA total. Has
  `coefficients`, `feature_names`, `intercept`, `alpha`,
  `validation_residual_sd`, `locked_holdout`, `market_qualification`,
  `status`. This is the one with the documented negative-ROI evidence
  (see `config/production.yaml`'s `blocked_workflows` entry for
  `wnba-total-margin-v1`: MAE 13.1 vs 20.9, Brier 0.252 vs 0.369, **ROI
  −4.1%**, CLV rate 0.0, on 89 lined holdout games,
  `scripts/wnba_totals_market_residual_walkforward.py`).

Steps (not yet started):

1. Read `src/model_prediction/production_registry.py` fully — understand
   exactly what `market_models` schema a `json_artifact` entry needs to
   load `available=True`. Compare against `mlb-elo-trend-lr-v8.json`'s
   `market_models.moneyline` block (the one working example) and
   whatever in `learned_forward.py` actually reads that block at predict
   time.
2. Figure out how these four artifacts' real prediction logic is invoked
   TODAY — `measured-edge-*` map to `MeasuredEdgeMarginModel` /
   `MeasuredEdgeTotalsModel` (see CLAUDE.md's "MLB distribution methods"
   section: `simulate_game(method=...)`, `compare_distribution_methods()`
   in `mlb.py` or wherever those classes live — grep for
   `MeasuredEdgeMarginModel`/`MeasuredEdgeTotalsModel`). wnba-spread/total
   have inline CDF/coefficient formulas already in the JSON — check if
   any code already evaluates those formulas (grep for
   `wnba-spread-margin-v1`/`wnba-total-margin-v1` model_version strings
   in `src/`) or if serving them needs new glue code reusing existing
   math. **Do not invent a new serving path that doesn't already exist in
   some form** — if real prediction-serving code exists for these, wire
   the registry to it (probably `implementation: code_backed` pointing at
   an existing Python entrypoint, like soccer/tennis in
   `config/production.yaml`, rather than forcing a `json_artifact`
   `market_models` block that isn't actually consumed by anything).
3. Add `sport` fields + correct contract shape (either
   `market_models`/`json_artifact`, or `implementation: code_backed` +
   `entry`) so each of the 4 resolves `available=True`.
4. Register all 4 in `config/production.yaml`'s `models:` list.
5. `model_promotion.promote()` each into `champions`:
   - MLB / total → `measured-edge-totals-v3`
   - MLB / spread → `measured-edge-margin-v3`
   - WNBA / spread → `wnba-spread-margin-v1`
   - WNBA / total → `wnba-total-margin-v1`
   Pass `market_evidence_id` pointing at the real evidence that already
   exists (e.g. reference the `blocked_workflows` note text / the
   walk-forward script name for WNBA total; whatever diagnostic evidence
   backs the MLB measured-edge models — check `docs/ROADMAP.md` /
   `docs/PROJECT_STATUS.md` for what evidence already exists for these).
   Evidence being unfavorable (WNBA total) is fine and expected per the
   operator's override — do not fabricate favorable numbers.
6. Remove these 4 entries from `config/production.yaml`'s
   `blocked_workflows` list (no longer blocked once promoted) — leaving a
   promoted model listed as blocked would make `system_health.py` report
   a contradictory DEGRADED status (it degrades health when
   `blocked_workflows` is non-empty).

### Task 2 — build + promote a real MLB first-inning (NRFI/YRFI) model

`config/models/mlb-nrfi-v1.json` **does not exist on disk** — confirmed
this session (`FileNotFoundError`). It's a placeholder name in
`blocked_workflows` only. Code exists at:
- `src/model_prediction/models/mlb_first_inning.py`
- `src/model_prediction/models/mlb_first_inning_sim.py`
- `src/model_prediction/models/mlb_nrfi.py`

Prior-session research context (from `docs/HANDOFF_2026-08-27.md`, the
FIRST handoff written today, from the market-edge execution session):
NRFI challengers were already run and came back as **honest nulls**:
half-inning decomposition (+0.0003 logloss, CI straddles zero) and
shrunk plate-umpire features (direction negative) — reproduction gate
PASS (0.690434 vs 0.6910 baseline). Also: **Polymarket US lists no
NRFI/YRFI markets at all** (F5 only) — so there is currently no live
market to serve NRFI predictions against, and no real closing-line
evidence source for it. The seeded PA first-inning simulator (Phase E,
commit a439987) also came back negative (0.7353 vs 0.6904).

Steps (not yet started):

1. Read the three `mlb_*first_inning*`/`mlb_nrfi.py` files, plus
   `docs/ROADMAP.md`'s Phase C NRFI section and `docs/PROJECT_STATUS.md`
   for the most complete existing walk-forward/ablation result for this
   model family — there should already be a baseline NRFI model
   (0.6910-ish logloss) that predates the challengers, per the
   "reproduction gate PASS (0.690434 vs 0.6910)" note above. That
   baseline is the real candidate to freeze into `mlb-nrfi-v1.json`, not
   the negative challengers.
2. Follow `docs/ARCHITECTURE.md`'s validation contract (60/20/20
   chronological split via `validation.py::build_walk_forward_rows`/
   `chronological_split`) to produce a real frozen artifact with real
   parameters — do not fabricate. If the baseline's numbers are only
   available via a script run (not already a JSON artifact anywhere),
   re-run the relevant script (likely under `scripts/`, grep for
   `nrfi`) to regenerate the real coefficients, or locate them in
   existing research output. Be explicit in the promotion evidence about
   exactly which run/commit backs the artifact.
3. Check `domain.py`'s `MarketType` enum for the real NRFI market-type
   value (referenced elsewhere as `market: nrfi` in
   `config/production.yaml`'s existing `blocked_workflows` entry).
   Register `mlb-nrfi-v1` in `config/production.yaml`'s `models:` list
   with a correct `market_models.nrfi` (or whatever the enum names it)
   contract, then `model_promotion.promote()` it as MLB/nrfi champion
   with real `market_evidence_id`.
4. **Important caveat to flag to the operator when this resumes**: since
   Polymarket has no NRFI market, "production" here means the model will
   generate shadow calls with no live market to price against or grade
   CLV/ROI on market terms — it's real serving in the sense of appearing
   as a champion and generating ledger rows, but the market-relative
   battery (`market_health.py`) will have nothing to compare it to for
   this market. That's a real, worth-surfacing limitation, not a reason
   to block — just say it plainly rather than silently proceeding.

## Constraints (from CLAUDE.md + this session's directives)

- Comments explain *why*, not *what* — match existing style exactly.
- Do NOT touch real-money order execution code, ledger-write mutation
  logic (`archive_settled_rows`/`remove_open_rows` only), or unit-sizing
  logic — this is registry/config/promotion wiring + (task 2) one new
  frozen artifact, nothing else.
- After each task: `env PYTHONPATH=src:. .venv/bin/python -m pytest
  tests/ -q` (full suite, ~4 min, that's normal) and `.venv/bin/ruff
  check src/ tests/` must both stay green/clean. Add/update tests
  following `tests/test_production_registry.py` and
  `tests/test_model_promotion.py` patterns (see this repo's own recent
  commit `a82b5b3` for the style of promotion/registry tests just added).
- `config/production.yaml`'s `champions` map and `blocked_workflows` list
  must stay mutually consistent — `system_health.py` reads both.
- Git: commit each task separately (2 commits, not 1), push to `origin
  main` after each — don't hold everything uncommitted till the end.
  Normal commit message style, `Co-Authored-By: Claude Sonnet 5
  <noreply@anthropic.com>`. No force-push, don't touch other branches.
- If any sub-step is genuinely impossible without fabricating data
  (e.g., truly no real historical result exists to back the NRFI
  artifact), stop that sub-step, say so plainly, keep going on the rest.

## Process note for the next session

The operator asked me to delegate this to a background fork agent (which
also auto-commits/pushes). The auto-mode permission classifier denied
that three times in a row with "Stage 2 classifier error" — not
obviously transient (denied identically on retry). **Do this work
inline in the main conversation**, not via a background `Agent`/fork
call that pushes autonomously, unless the operator has since granted a
Bash permission rule for it.
