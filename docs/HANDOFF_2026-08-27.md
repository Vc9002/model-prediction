# Handoff — 2026-08-27 market-edge execution session

Branch `research/mlb-v9`. Six commits (cb9ee38 → a439987), all unpushed.
This session executed the operator's market-edge architectural directive
("execute this plan") — full detail in the commit messages and the
"Market-edge execution program" section of `docs/ROADMAP.md`.

## Done (all committed)

1. **Phase A — Market-relative evaluation foundation** (cb9ee38):
   `src/model_prediction/market_eval.py` (Δlogloss/ΔBrier vs no-vig
   market, CLV, ROI at executable asks, profit factor, drawdown,
   date-clustered bootstrap CI, edge buckets) + market-data census
   script. Census: mlb/wnba/esports/soccer/tennis snapshots fresh
   through 08-26; NBA/NFL absent; zero NRFI/YRFI slugs anywhere; kbo/npb
   moneyline-only.
2. **Phase B — WNBA totals vs market** (4db2160): reproduction gate
   PASS (19.8817 vs 19.9). On 89 lined holdout games the market line
   beats the incumbent decisively — MAE 13.12 vs 20.92, Brier 0.252 vs
   0.369 (CI entirely positive), ROI −4.1%, CLV rate 0.0, ECE 0.357.
   The apparent +0.35 "edge" is overconfidence. Residual probe (45
   train rows) does not transfer. Also fixed a real `calibration.py`
   IRLS `math.exp` overflow (clamped, regression-tested).
3. **Phase B2 — Structural challenger** (21d0f3e): new PIT player-log
   feed (`features/wnba_player_logs.py`, two real-schema parser bugs
   pinned by tests) + three challenger features in
   `build_total_score_rows` (flag-gated, incumbent byte-identical).
   Honest null: unlearnable under the artifact split (boxscore captures
   start ~2026-07); market-window probe (49/25) shows no transfer
   (22.30 vs 21.24).
4. **Phase C — NRFI challengers** (fc25f01): half-inning decomposition
   (+0.0003 logloss, CI straddles zero) and shrunk plate-umpire
   features (direction negative) — both honest nulls; reproduction gate
   PASS (0.690434 vs 0.6910). Probe result: Polymarket US lists no
   NRFI/YRFI markets (F5 only); no innings-1-5 data anywhere for F5
   targets; TTO1 needs Statcast split ingestion. All three recorded as
   data-blocked in ROADMAP.
5. **Phase D — Governance** (579b12f): Phase 23 gates rewritten
   (market-relative predictive gate, full economic battery primary,
   stability slices, hit-rate diagnostic-only, beta calibration,
   per-market×league calibration, rolling walk-forward; 80/90 conflict
   resolved in favor of 80). `calibration.BetaCalibrator` +
   `validation.rolling_walk_forward_splits`, both tested.
6. **Phase E — Simulator + stack/σ** (a439987): seeded PA first-inning
   simulator (one-time calibration to league NRFI prior 0.5106) —
   challenger verdict negative (0.7353 vs 0.6904, level-term overshoot
   in the current regime). Stack/σ probe in the WNBA window: per-game σ
   cuts Brier 0.445→0.357 and the stack learns to weight the market
   line (+1.28) — positive mechanisms, market still ahead (n=22).

## Not finished / open

- **Operator decisions pending**: API-FOOTBALL key (unchanged);
  NBA/NFL odds source (TheRundown shortlist, unchanged); real NRFI
  quotes need a non-Polymarket provider if wanted (documented blocked).
- **Data depth is the binding constraint everywhere**: the Polymarket
  snapshot tree covers ~6 weeks, so every market-relative model trains
  on ~45 rows. The mechanisms are in place; the evidence will come from
  accumulating snapshots.
- **ROADMAP.md is uncommitted** (contains this program's status section
  plus the prior session's night-continuation edits — commit them
  together or split deliberately).
- **Prior session's uncommitted WIP remains in the working tree**
  (tennis derivatives research, bet_better, statcast aggregates,
  wnba totals signals harness, fixtures, etc.) — untouched by this
  session's commits.
- TTO1 Statcast splits, confirmed-lineup NRFI wiring, and the F5 model
  remain blocked on data capture (see ROADMAP phase C).

## Notes

- All research-only: no production artifacts touched, no ledger writes,
  no promotion. v8 frozen invariant respected throughout.
- Six commits on `research/mlb-v9`, unpushed (repo norm).
- Observation log entries #83–85 (plan reconciliation, market-as-arbiter,
  challenger-identical-to-incumbent is a coverage failure).
