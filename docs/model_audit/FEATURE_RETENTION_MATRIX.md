# Feature Retention Matrix — Model/Feature Reconciliation Audit

**Date**: 2026-08-11
**Scope**: MLB, NBA, WNBA, NFL, Soccer, Tennis (the six priority sports per the audit
mandate). Esports (LoL/CS2/Dota2/RainbowSix/Valorant) and KBO/NPB are historical controls
outside this round's priority — audit them later using the same process (see
`docs/model_audit/MODEL_INVENTORY.md` for their current inventory entries).

This is the consolidated cross-sport view. Full per-model and per-feature evidence, with
file:line citations, lives in `docs/model_audit/models/*.md` and
`docs/model_audit/features/*.md`. `config/tested_features.json` and
`docs/FEATURE_REGISTRY.md` carry the corrections this audit made to the shared registry.

## Final audit matrix

| Sport | Model | Action | Feature Action | Calibration | Priority |
|---|---|---|---|---|---|
| MLB | `mlb-elo-trend-lr-v8` (ML) | KEEP_PRIMARY / HISTORICAL_CONTROL — `qualified: false` on its own artifact (validation Brier regressed even though holdout hit rate looked better); do not treat as proven best. **Also: live artifact's threshold (0.619665, 148 calls, 60.8%) does not match what `MASTER.md`/`docs/PROJECT_STATUS.md` describe as current (0.587335, 352 calls, 58.5%) — that "fix" commit exists in repo history but was never merged into main. Needs operator reconciliation before any further v8 work.** | Audit six features first (elo_probability, trend_gap, park_factor, weather_factor, starter_era_gap, bullpen_weakness_gap) before adding new ones. `bullpen_weakness_gap` registry entry corrected (was stale "untested", is actually live-wired with fitted coefficients) — formal with/without ablation still open. `starter_era_gap_live`/`starter_fip_gap` are real PIT-safe dormant scaffolding, RETEST_REQUIRED, not yet promoted. Legacy `starter_era_gap` (event-id map) stays permanently RETIRED. | No `calibration` sub-object in current artifact at all — unlike NBA/WNBA/NFL. Add one. | HIGH |
| MLB | `mlb-elo-trend-lr-v7` | KEEP_ROLLBACK | n/a (frozen) | n/a | HIGH |
| MLB | `mlb-analyst-poisson-trend-v0.3` (spread/total) | KEEP_BASELINE / control | Improve run-intensity component for totals; retain architecture. | Audit separately from ML calibration. | HIGH |
| MLB | Clean-slate XGBoost two-head + negative-binomial | KEEP_CHALLENGER, not a moneyline replacement | Coherent score/spread/total/expected-score research engine. Corrected benchmark (LogLoss 0.6927 vs naive-constant 0.6931) is real and too thin a margin to replace v8. | Already has cross-fit calibration in the benchmark. | MEDIUM |
| NBA | `nba-elo-trend-lr-v4` (ML) | **KEEP — Elo integrity CONFIRMED (no leakage), previously-blocking question now resolved.** See `docs/model_audit/models/NBA_ELO_TREND_LR_V4.md`. | `elo_probability` KEEP_CORE (coef 3.564). `trend_gap` RETEST_REQUIRED (coef -0.0036). `defensive_trend_gap` REMOVE for a future v5 only (coef -0.013, v4 stays immutable). Candidates `consistency_gap`/`hot_cold_gap`/`rest_disparity`/`games_last_7_gap`/`schedule_missingness` all exist as real, PIT-safe, wired-but-inert code already — RETEST_REQUIRED, not "build from scratch." Note: tracked feature name `schedule_missingness` is actually implemented as `schedule_available` in code — naming mismatch to fix when promoting. | Not yet applied at serving time; recommend identity/Platt/temperature/isotonic cross-fit comparison next. | HIGH |
| WNBA | `wnba-elo-trend-lr-v4` (ML) | KEEP | `elo_probability` KEEP_CORE. `trend_gap` RETEST_REQUIRED. `defensive_trend_gap` REMOVE for a future v5 (coef -0.0029, **sign-flipped from v3's +0.0090** — stronger evidence of noise than a stable near-zero coefficient). `availability_points_gap`/`player_availability` KEEP (grade-B evidence) but has an unresolved numeric contradiction with `docs/MODEL_IMPROVEMENTS.md` on the same cohort — needs reconciliation before being cited as settled. Archived `rebuild/wnba-v1` efficiency features (ORtg/DRtg/NetRtg/pace/eFG/TOV%/ORB%/FT-rate, PIT-safe, multi-window) recommended RECOVER as v5 candidates — excluded from the original merge as a scope decision, not a quality rejection. | Not yet applied; same cross-fit comparison recommended as NBA/NFL. | HIGH |
| WNBA | `wnba-spread-baseline-v1` (spread/total) | KEEP_BASELINE / REBUILD_IMPROVE_HEAD_REQUIRED — confirmed weak: spread side actively losing (-126 units, 32.9% hit rate). Total side's 78.3% needs methodology scrutiny before trusting. | Archived `baselines.py` (margin Ridge / total Ridge / residual covariance) is AUDIT_ONLY, not RECOVER — self-gates to `production_allowed: False` pending unresolved commercial-data rights. | n/a yet | MEDIUM |
| NFL | `nfl-elo-trend-lr-v4` (ML) | KEEP | `elo_probability` (coef 2.615), `trend_gap` (coef 0.050) — feature set confirmed exactly as expected, no changes recommended before calibration work lands. `head_to_head` retest condition confirmed verbatim: *"NFL only, and only after H2H coverage exceeds 50%. Never retest the other four sports."* nflverse EPA/success-rate pipeline exists on main but self-flags `retrospective_pit_qualified: False` / `production_allowed: False` — real, code-documented blocker, not neglect. | **CALIBRATE FIRST — confirmed from two independent sources: artifact's own `expected_calibration_error: 0.1009` (n=87) and `tested_features.json`'s own note calling this "the worst ECE" of the tracked models. Tooling (`rebuild/calibration.py`: Identity/Platt/Isotonic/Temperature + chronological `cross_fit_calibration_eval`) already exists and has a ready MLB template to copy — never run for NFL yet.** | HIGH |
| Soccer | `soccer-poisson-dc-v1` | KEEP strongly — coherent Poisson + Dixon-Coles architecture, one of the least-worth-replacing models in the project. **Live bug found: `rebuild-shadow --sport soccer` still routes this genuinely 3-way sport through a binary-Elo baseline adapter (`_BasicEloAdapter`) instead of the real model. A fix exists on unmerged `origin/rebuild/soccer-v1` but was never ported to main.** | `HOME_GOAL_BOOST=1.15` and `DC_RHO=-0.10` confirmed hardcoded, textbook-plausible but never fitted via MLE/grid-search anywhere in the repo — REBUILD_IMPLEMENTATION candidate (fit them, don't just keep guessing). One global home-advantage/baseline applied uniformly across all 19 configured leagues — competition-specific attack/defense is a real, untested candidate. | **Confirmed not uniform: BTTS has a real holdout-verified Platt fix (55.0%→56.7%). 1X2 and total have NO calibration at all — raw-probability argmax, not even binarized — despite total being soccer's primary priced market. Fix total calibration next.** | HIGH |
| Tennis | `tennis-surface-elo-v1` | KEEP — do not rebuild. Real, active, shadow_qualified (65.5% hit rate, 4,269 locked-holdout calls, +1,070.7 units), but that qualification evidence lives only in a YAML comment in `config/model.yaml`, not a reproducible artifact — every other shadow-qualified league has one; add it. | **Confirmed registry bug, not just staleness: `tennis_surface = exclude` reason ("no active model consuming it") was false — the live model blends surface Elo at 60% weight into every match probability (`models/tennis.py:59-97`). Corrected in `tested_features.json`/`FEATURE_REGISTRY.md` this audit.** Data-foundation assumption corrected too: current Tennis is fed by **ESPN only** (`data_sources/espn.py` via `ingest.py`), not "TennisMyLife + ESPN" as assumed going in — TennisMyLife is real but lives only in the unpromoted `rebuild/tennis/` track with `production_allowed: False` pending licensing. Sackmann docstring reference in `models/tennis.py:3` is real but confirmed harmless dead code, doesn't affect runtime. | Not yet audited/applied — K_FACTOR=32, surface_weight=0.60 not yet independently re-derived this round, carried forward as-is. | HIGH |

## Registry corrections made this audit (see `config/tested_features.json` / `docs/FEATURE_REGISTRY.md` diffs)

1. `tennis_surface` — reason corrected (see Tennis row above); verdict (`exclude`) unchanged and still correct for that one specific dead function.
2. `bullpen` — corrected from stale "untested/data source passes None" to `keep`/live-wired, with fitted-coefficient evidence.
3. `defensive_trend_gap` — added WNBA v3→v4 sign-flip evidence, strengthening (not changing) the existing `remove_candidate` verdict.
4. `player_availability` — flagged an unresolved numeric contradiction with `docs/MODEL_IMPROVEMENTS.md` rather than silently trusting either document.
5. Added a `starter_fip_gap` entry to `tested_features.json` (existed in the human-readable `FEATURE_REGISTRY.md` already but was missing from the machine-readable source of truth — now consistent).
6. `docs/FEATURE_REGISTRY.md`'s "unresolved question that outranks all feature work" (NBA Elo leakage) — **resolved**, replaced with the audit's findings.

## Completion-gate status (master audit doc §106)

Done, six priority sports: model inventory, archive recovery map, clean-slate MLB
challenger card, per-sport model cards and feature docs, stale-registry corrections,
model/feature implementation-mismatch identification (starter_era_gap legacy vs live),
calibration state per model, no serving behavior changed.

Not done / open, flagged honestly rather than claimed complete:
- **Rollback model isn't uniformly established for every sport** — MLB has `v7`; NBA/WNBA/NFL/Soccer/Tennis do not have an equally explicit, audited rollback artifact identified in this round. Needs a follow-up pass before any same-family refit ships.
- Every-feature PIT/train-serve-parity status is thorough for the six priority sports' *active* features but not exhaustively re-traced for every dormant/candidate feature mentioned (e.g. the full `schedule_load` sub-feature family reuses prior roadmap-challenger evidence rather than an independent re-trace this round).
- Esports/KBO/NPB (`tiered-elo-v6`, `tie-aware-elo-v1`) are inventoried in `MODEL_INVENTORY.md` but not audited to this same depth — explicitly deferred, per the master doc's own sequencing.
- Two open contradictions are recorded, not resolved: the MLB v8 threshold/doc mismatch (see MLB row above) and the WNBA player-availability cohort-number mismatch. Both need an operator decision, not another audit pass.
