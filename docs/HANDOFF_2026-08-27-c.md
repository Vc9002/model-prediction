# Handoff — 2026-08-27 (third session) — Task 1 done + pushed, Task 2 (NRFI) not started

Repo clean at commit `96b2946` (production: wire MLB/WNBA spread+total into
shadow-ledger production — pushed). This session picked up
`docs/HANDOFF_2026-08-27-b.md`'s two tasks; **Task 1 is complete and
pushed. Task 2 (MLB NRFI) is untouched — no files edited.** Ran out of
context budget partway through Task 2 investigation; stopping cleanly
rather than rushing a frozen artifact. Pick up Task 2 fresh from here.

## Task 1 — DONE (commit `96b2946`, pushed to origin/main)

Registered and promoted to champion:
- MLB spread → `measured-edge-margin-v3`
- MLB total → `measured-edge-totals-v3`
- WNBA spread → `wnba-spread-margin-v1`
- WNBA total → `wnba-total-margin-v1`

Key discovery (read this before touching production.yaml again): the
actual serving gate for MLB/WNBA spread+total is
`_is_registered_serving_model()` in `src/model_prediction/cli/forecast.py`,
which calls `ProductionModelRegistry.champion(sport, market)` — used by
`_forecast_wnba_spread_sport`/`_forecast_wnba_total_sport`/
`_forecast_mlb_totals_flat` (all invoked from `daily.py`'s research pool)
to decide Main-ledger (`CALL`) vs `_downgrade_unserved()`
(`PAPER_CALL_*`, research-only). **This is separate from
`cli_production.py`'s `_cmd_predict`**, which only ever serves ONE single
primary model (currently WNBA moneyline) via `build_learned_moneyline_slate`
— that's the scheduled "Production Canary" cycle, not where MLB/WNBA
spread+total picks reach Main. Don't confuse the two paths.

All 4 artifacts' `artifact_hash` already matched their payload (no artifact
file edits needed) — `production.yaml`'s `models:`/`allowed_models:`/
`artifact_map:` additions plus explicit `champions:` pointers were
sufficient. `ProductionModelRegistry.champion()` actually falls back to the
unique registered entry for a sport+market when no explicit `champions:`
pointer exists, so `model_promotion.promote()` refused all 4 as "already
serves" — the explicit `champions:` entries were added by direct yaml edit
instead (matches the pattern every other sport/market already uses), not
via `promote()`. This is a real edge case `promote()`'s guard doesn't
handle (first-time activation of a fallback-resolved model, not a
reassignment) — not fixed this session, out of scope.

Also fixed: `model_promotion.py`'s CLI `promote` subcommand never exposed
`--market-evidence`/`--market-evidence-unavailable-reason`, so the Phase-23
gate added the prior session made the documented CLI promotion path
unusable. Added both flags (`main()` now passes them through to
`promote()`).

Tests: `tests/test_production_registry.py` and `tests/test_ci_contracts.py`
had hardcoded expectations pinning the old blocked state (registry entry
count `13`, explicit `not in blocked_workflows` assertions for the 4
models) — updated to `17` and the new expected state. Full suite: 2403
passed, 3 skipped, ruff clean.

Note: auto-mode permission classifier denied the `model_promotion.promote`
CLI call once on first attempt, then succeeded identically on retry with no
change — appears transient/flaky this session (contrast with the prior
session's handoff, which reported 3/3 identical denials). If it blocks
again, retry once or twice before treating it as a hard wall.

## Task 2 — MLB NRFI artifact — NOT STARTED (do this next)

Goal (unchanged from `docs/HANDOFF_2026-08-27-b.md`): freeze the real
baseline NRFI model (~0.6910 logloss, predates the negative challengers)
into `config/models/mlb-nrfi-v1.json`, register + promote it as MLB/nrfi
champion, remove it from `blocked_workflows`. Do NOT fabricate parameters.

What this session found before stopping:

- `src/model_prediction/models/mlb_nrfi.py`'s `MLBNRFIModel` class has a
  `to_dict()`/`from_dict()` round-trip with hardcoded `weights`/
  `intercept`/`decomposed_blend_weight` defaults — this is *plausibly* the
  shape a frozen artifact should take, but **it is not yet confirmed
  whether these hardcoded defaults are the actual validated ~0.6910-logloss
  baseline, or just placeholder/untrained values.** Verify before freezing
  anything into JSON.
- Files not yet read: `src/model_prediction/models/mlb_first_inning.py`
  (904 lines) and `mlb_first_inning_sim.py` (199 lines) — per the prior
  handoff these have "the PIT feature ledger, hierarchical credibility
  shrinkage, top-of-order composite, platoon share, starter rest." Read
  these before `mlb_nrfi.py` conclusions are trusted — the baseline may
  live in one of these instead.
- Candidate scripts that reference the `0.6910`/`0.690434` figures (not yet
  opened this session): `scripts/mlb_nrfi_half_inning_challenger.py`,
  `docs/DEBUG.md`, `docs/ROADMAP.md` (Phase C section, already quoted in
  `HANDOFF_2026-08-27-b.md`), `outputs/roadmap_challenger/
  ROADMAP_CHALLENGER_DECISION_DOSSIER.md`. Start with the challenger
  script — it's the most likely place to find exactly what baseline model
  it reproduces against and how that baseline's parameters were derived.
- `_forecast_mlb_nrfi_flat` in `src/model_prediction/cli/forecast.py`
  (function starts ~line 616) is the existing daily research-tier caller —
  read it to see which model class/artifact it currently instantiates
  in-code, matching Task 1's pattern of "find what's already really
  serving before adding new config."

Steps (from the original handoff, still accurate):

1. Read the three `mlb_*first_inning*`/`mlb_nrfi.py` files fully, plus
   `docs/ROADMAP.md`'s Phase C NRFI section and `docs/PROJECT_STATUS.md`,
   to find the real walk-forward/ablation result backing ~0.6910 logloss.
2. Follow `docs/ARCHITECTURE.md`'s validation contract (60/20/20
   chronological split) — reuse `validation.py::build_walk_forward_rows`/
   `chronological_split`, don't hand-roll. If the baseline's real
   coefficients only exist via a script run (not already saved as JSON),
   re-run the script to regenerate them, and cite exactly which
   run/commit backs the artifact.
3. Check `domain.py`'s `MarketType` enum for the real `nrfi` market-type
   value. Register `mlb-nrfi-v1` in `config/production.yaml`'s `models:`
   list (mirror the same `allowed_models:`/`artifact_map:` update pattern
   Task 1 used — `_validate_explicit_mirrors` in
   `production_registry.py` enforces these stay in exact sync), add an
   explicit `champions.MLB.nrfi:` pointer, then remove the `mlb-nrfi-v1`
   entry from `blocked_workflows`.
4. Promote via `model_promotion.py promote --new mlb-nrfi-v1 --sport MLB
   --market nrfi --approved-by operator --market-evidence-unavailable-reason
   "..."` (Polymarket has no NRFI market — no market_eval report exists,
   same honest-unavailable-reason pattern Task 1 used). Note: given the
   `champion()`-fallback quirk found in Task 1, if `mlb-nrfi-v1` ends up
   the sole registered NRFI entry, `promote()` may again refuse as
   "already serves" — fall back to a direct yaml `champions:` edit like
   Task 1 did, and say so plainly rather than silently working around it.
5. **Flag to the operator**: Polymarket has no NRFI/YRFI market at all —
   this will generate shadow ledger rows with nothing to grade CLV/ROI
   against on market terms. Real serving in the sense of appearing as
   champion + generating rows, but `market_health.py`'s battery will have
   no comparison for this market. Say this plainly when it resumes, don't
   bury it.
6. Same test/lint/commit/push discipline as Task 1: full suite + ruff
   clean, update any hardcoded registry-entry-count assertions (this
   session's Task 1 already bumped `13` → `17`; Task 2 will need `17` →
   `18`), commit separately from Task 1's work (already pushed), push to
   `origin main`.

## Constraints (unchanged, still binding)

- Comments explain *why* not *what*; match existing style.
- Don't touch real-money order execution, ledger-mutation internals
  (`archive_settled_rows`/`remove_open_rows` only), or unit-sizing logic.
- `env PYTHONPATH=src:. .venv/bin/python -m pytest tests/ -q` (~7 min, that's
  normal) and `.venv/bin/ruff check src/ tests/` must both stay clean.
- `config/production.yaml`'s `champions:` map and `blocked_workflows:` list
  must stay mutually consistent (`system_health.py` reads both).
- Commit + push after Task 2, same message style + `Co-Authored-By: Claude
  Sonnet 5 <noreply@anthropic.com>`. No force-push, don't touch other
  branches.
- If any sub-step is genuinely impossible without fabricating data, stop
  that sub-step, say so plainly, don't fabricate.
