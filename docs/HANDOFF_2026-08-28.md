# Handoff — 2026-08-28 (NRFI-promotion session; note: a parallel audit session also wrote HANDOFF_2026-08-27-e.md — this file is THIS session's record)

Repo state: origin/main at `8e490f0`. Commits pushed this session:
`cceef15` (NRFI artifact freeze + roadmap backlog), `693744b` (NRFI
production wiring), `f8133a5` (weather/travel research), `8e490f0`
(operator notifications off). Full suite 2414 passed, 3 skipped; ruff
clean at each commit.

## 1. MLB NRFI — promoted for real (the landmine from HANDOFF_2026-08-27-d is fixed)

- `models/mlb_first_inning_live.py`: live PIT feature builder for the 19
  first-inning features, same formulas as `build_first_inning_ledger` but
  keyed by starter NAME (accepted-risk pattern from
  `features/starter_history.py` — no ESPN-athleteId → StatsAPI-playerId
  crosswalk exists). Exact-match verified vs the batch ledger on 7 real
  games across 3 seasons; parity test `tests/test_mlb_first_inning_live.py`
  with 1e-4 tolerance (one documented floating-point rounding artifact).
- `_forecast_mlb_nrfi_flat` fails closed on missing/hash-invalid artifact,
  predicts with `MLBFirstInningModel.from_dict(artifact)`; legacy
  `MLBNRFIModel` no longer on the serving path.
- `config/production.yaml`: `mlb-nrfi-v1` registered, champion `MLB.nrfi`,
  `blocked_workflows` now `[]`.
- Caveats: Polymarket has no NRFI market (no market-side CLV/ROI grading);
  legacy `MLBNRFIModel.model_version` still defaults to "mlb-nrfi-v1" —
  grep both classes before touching NRFI again.

## 2. Venue / weather / travel research — built, ablated, NOT promoted

- `features/mlb_venue_geocoding.py`: venue-name-keyed static table (35
  venues; relocation-correct; one-off exhibition venues excluded).
- `scripts/backfill_mlb_weather.py` + `data/weather/mlb_historical.jsonl`
  (untracked): raw Open-Meteo archive hourly observations; ~5,800/5,899
  venue-days ok; **52 pairs still http_429 after two retry passes** — one
  more `--workers 2` resume run clears most.
- `features/mlb_game_context.py` + `total_score.py` `real_mlb_context`
  flag (default OFF = byte-identical incumbent).
- `scripts/mlb_weather_travel_ablation.py`: reproduction gate PASS (control
  reproduced pre-wiring MAE 3.584296 / RMSE 4.487904 exactly), verdict
  **NO_PROMOTION** — candidate MAE gain 95% CI [-0.000215, +0.011088]
  fails the 0.005 minimum-effect threshold. Report (gitignored):
  `outputs/latest/mlb-weather-travel-ablation.json`.

## 3. Odds API audit (code-level)

`data_sources/the_odds_api.py` = in-season endpoints only (`odds`/`scores`/
`tennis_odds`); no `/v4/historical/` client; `x-requests-remaining` never
read. Historical snapshots are a PAID tier; whether the existing key can
reach them was NOT tested (live test blocked by the `Read(.env)` deny
rule — don't work around it). Operator decision needed before any
historical-market warehouse work. Cheap win regardless: log
`x-requests-remaining` in `_safe_get`.

## 4. Operator notifications — killed and removed (`8e490f0`)

`run_supervisor.notify_operator` now returns early when
`MODEL_PREDICTION_OPERATOR_NOTIFICATIONS` is falsey; all four launchd
plists carry `=0` and were bootout+bootstrap reloaded (dashboard env
verified live). Re-enable by flipping the env var.

## 5. Documentation sync

Corrected the parallel audit session's docs to reflect this session's
outcomes (NRFI promoted, not gated) across PROJECT_STATUS / DEBUG / MASTER
/ ROADMAP / SYSTEM_DEFECTS_AND_GAPS_AUDIT; added weather-backfill state,
venue table, Odds historical-tier findings. The parallel session
overwrote this session's first attempt at `HANDOFF_2026-08-27-e.md` with
its own content — both records now coexist under distinct filenames.

## Repo state notes

- Auto-mode classifier flaky again (`git add config/production.yaml`
  denied twice then allowed; transient classifier timeouts).
- Pre-commit ruff-format aborts commits after reformatting staged files —
  re-stage + re-commit; second attempt passes.
- Handoff docs remain uncommitted by convention.

## Still open

- 52 rate-limited weather pairs (one more resume run).
- Odds API key-tier check + operator decision on paid historical access.
- TheRundown parallel capture (free tier) — no credential requested yet.
- Mypy hardening (220 findings cataloged by the audit session).
