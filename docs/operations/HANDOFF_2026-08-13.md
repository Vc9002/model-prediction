# Handoff — 2026-08-13 deep audit + fix pass + scheduler restoration

Written at end of the 2026-08-13 session. Everything below was verified live;
where an item is open, the exact next step is given.

## What happened

1. **Deep audit** of the incumbent + rebuild systems (10-step DEBUG.md
   procedure): found and fixed 13 real bugs (F-72..F-84, full evidence in
   `MASTER.md`'s 2026-08-13 session entry). Committed as `53aa86f` with the
   Main-ledger un-retire (operator directive).
2. **Scheduler restoration** (operator directive): both launchd agents
   bootstrapped; production canary verified working end-to-end;
   `run_rebuild.sh` found mislabeled (it ran the INCUMBENT forecast, not the
   rebuild pipeline) and rewritten.
3. **MLB v9 ablation re-run** with a metrics-first harness (operator
   directive). The **v8 reproduction gate FAILED** — see below. Per the
   operator's standing instruction: **do not use these ablation results for
   any promotion decision.**

## Git state

- `main` at `53aa86f` ("fix: deep-audit fixes F-72..F-84, un-retire main
  ledger, wire production ledger"). **3 commits ahead of origin/main —
  not pushed.** Push is a deliberate next step, not done automatically.
- Uncommitted working-tree changes (this session, all tested):
  - `src/model_prediction/cli_production.py` + `dashboard/production.py` —
    production split-brain fix: state/ledger/FeatureStore now anchored to
    repo `data/` (deliberately NOT env-var-aware; runtime root is
    rebuild-scoped only per `MIGRATION_MANIFEST.json`).
  - `tests/test_production_ledger.py` — path-monkeypatch helper updated to
    the new `_resolve_data_root` (33 tests green).
  - `scripts/run_rebuild.sh` — rewritten to run `rebuild-shadow` per
    enabled sport instead of the incumbent `forecast --all --log`.
  - `src/model_prediction/validation.py` — `evaluate_variant` extended:
    `holdout_all_rows` (log loss/Brier/ECE/calibration slope+intercept over
    ALL holdout rows), `coverage`, `per_split` fold metrics.
  - `scripts/mlb_v9_ablation.py` — metrics-first summary table +
    v8 reproduction gate.
  - Data churn (routine): `data/main/*.xlsx` (Main un-retired + daily job),
    `data/production_state.json`, `data/production/predictions.db` (3 real
    canary predictions + reconciled run row), experiment log.

## Live services (verified)

| Service | State |
|---|---|
| `com.modelprediction.daily` | running every 3h, exit 0, Main re-enabled |
| `com.modelprediction.production` | **loaded + running every 3h**; verified real predictions (3 today, event 401857139 etc.), state in `data/production_state.json`, ledger `data/production/predictions.db`, health HEALTHY |
| `com.modelprediction.rebuild-shadow` | **loaded**; `RunAtLoad:false` (first run needs kickstart or waits ≤3h). Script rewritten; see open items |
| Dashboard | running manually on :8765, restarted with rebuild_status fixes, `/api/rebuild/status` ok |

Production ledger notes: 623 predictions / 2 run rows; backup from before
the test-pollution cleanup at `/tmp/predictions.db.bak` (4 fake predictions +
their run rows deleted with explicit operator authorization; the real
19:47:44 run row was accidentally caught by the time-window delete and
reconstructed from its own predictions — verified 0 orphaned predictions).

## MLB v9 ablation — headline result (do not act on it)

New metrics-first table written to `/tmp/mlb_v9_ablation2.out` (rerun is one
command). All 20 variants cluster extremely tightly on holdout all-rows
metrics (log loss 0.6840–0.6868, Brier ~0.2455–0.2468, ECE ~0.011–0.015,
calibration slopes ~0.88–1.00); fold spreads are small (~0.002–0.005). The
tiny spread means no variant meaningfully separates — consistent with the
clean-slate challenger result that benchmarked near a coin-flip baseline.

**v8 reproduction gate: FAILED.**

```
harness elo_trend_park_weather_starter_bullpen @0.60-target: calls=536, hit=0.5877, brier=0.2418
artifact mlb-elo-trend-lr-v8 qualification:              calls=148, hit=0.6081, brier=0.2464
reproduced_closely: False (call_ratio=3.622, hit_delta=0.0204)
```

The harness cannot reproduce v8's shipped qualification numbers on today's
data store (row set/date window have grown since the artifact's build; the
threshold selection also diverges — 536 vs 148 calls). Per the operator's
directive (2026-08-13): **stop here — do not evaluate residual trend, FIP,
K-BB% or any other variant for promotion until the corrected research
pipeline reproduces the incumbent.** Next step is pipeline reconciliation,
not feature judgment: pin the exact row set + threshold semantics v8 was
built with (`training_data_note` in the artifact qualification, the
`locked_complete_date_60_20_20` framework) and diff against what
`build_walk_forward_rows` produces today.

Also note: prior "residual-trend +56.4u" numbers are void (train/serve
parity fix F-79) and must never be cited.

## Open items (exact next steps)

1. **`run_rebuild.sh` enabled-sports derivation** — the heredoc reads
   `config/rebuild.yaml` (`rebuild.sports.<sport>.enabled`). A dry-run test
   from a non-repo cwd returned empty (config file not found — the script
   itself `cd`s to REPO_ROOT first, so likely fine). Verify from the repo
   root: `bash scripts/run_rebuild.sh` (or `launchctl kickstart
   gui/$(id -u)/com.modelprediction.rebuild-shadow`) and confirm per-sport
   lines in `~/model-prediction-runtime/logs/rebuild.log` for exactly
   mlb/wnba/nba/nfl/soccer/tennis.
2. **shadow.db freshness** — after the first (partially misconfigured) loop,
   `trade_decisions` is still 349, latest 2026-08-10. Confirm the corrected
   loop actually produces fresh trade_decisions; if the pipeline runs green
   but writes no decisions for today's slate, investigate whether that's
   "no qualifying markets" (honest) or a wiring gap. No shadow.db resets.
3. **Commit the uncommitted fixes** (production split-brain fix,
   run_rebuild.sh rewrite, validation/ablation harness extension) — separate
   commit from the already-landed `53aa86f`, then optionally push (3+ ahead
   of origin).
4. **Docs that still say the agents are unloaded** — `docs/PROJECT_STATUS.md`
   repair order #8 / scheduler table, `MASTER.md`'s 08-13 "still open" tail.
   Update after the agents have run a few clean cycles.
5. **v8 reproduction reconciliation** — see the ablation section above.
6. Do not re-cite the old v9 ablation numbers anywhere.

## Gotchas for the next session

- Run `pytest` WITHOUT the `MODEL_PREDICTION_*` env vars (documented form);
  with them, ~12 tests that pin the no-env default go red.
- `summary` returning zeros used to mean "Main retired"; Main is un-retired
  now — zeros mean something real is wrong.
- The isolation gate (`check_rebuild_isolation.py --base-ref origin/main`)
  fails locally while ahead of origin/main (expected — empty diff after
  push). `outputs/rebuild/verification.json` is gitignored and was
  regenerated locally from real junitxml/ruff/mypy evidence.
- `run_rebuild.sh` must only ever invoke `rebuild-shadow`; the incumbent
  forecast belongs to `run_daily.sh` only (isolation boundary).
- `data/main/*.xlsx` are tracked again (un-retire) — Aug 11–13 gap is
  historical, do not backfill without a directive.
