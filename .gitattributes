# No Git LFS tracking active (removed 2026-08-05 after repeated push timeouts).
# The two formerly-LFS files (data/events.jsonl, data/mlb_statsapi/game_snapshots.jsonl)
# are now gitignored — they're daily pipeline outputs that grow indefinitely
# and should never have been tracked.
