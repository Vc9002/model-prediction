# Pre-commit lint gate. Install once with: pre-commit install
# (DD-9: lint violations previously accumulated between CI runs.)
repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.16.2
    hooks:
      - id: ruff
        args: [--fix]
      - id: ruff-format
