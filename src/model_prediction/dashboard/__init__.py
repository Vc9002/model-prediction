"""model_prediction dashboard package.

The former dashboard_server.py monolith (DD-5) is decomposed by domain:
common (paths/cache/config), picks, evidence, status, matrix, backtests,
orders (includes order execution + portfolio/archive/activity state --
tightly coupled in the original file, kept together rather than forced
into a circular import), jobs, and routes (the HTTP Handler). This
package's __init__ is a pure re-export shim so `from model_prediction.dashboard
import _X` and the dashboard_server.py entrypoint keep resolving exactly
as before -- including the 35+ symbols the test suite imports/monkeypatches
directly.
"""

from __future__ import annotations

from .backtests import (  # noqa: F401 -- re-export for compat
    _lines_match,
    _pick_quote,
    _row_line,
    _row_matches_snapshot_event,
    _row_selected_team,
    _spread_side_for_row,
    _team_matches,
    _total_side_for_row,
    backtests,
    market_snapshots,
    odds_summary,
)
from .common import (  # noqa: F401 -- re-export for compat
    _ACTION_LOCK,
    _CACHE,
    _CACHE_LOCK,
    _CONFIG_LOCK,
    _DASHBOARD_CACHE,
    _DASHBOARD_TOKEN,
    _ENV_PATH,
    _FLAT_PICKS_CACHE,
    _JOBS,
    _JOBS_LOCK,
    _LAST_ACTION,
    _MAIN_LEDGER_SPORTS,
    _MARKET_QUESTION_CACHE,
    _MARKET_QUESTION_LOCK,
    _ORDER_LOCK,
    _ORDER_PREVIEWS,
    _PICKS_CACHE,
    _RUNNER,
    ARCHIVE_FILE,
    CONFIG_FILE,
    DASH_DIR,
    DASHBOARD_PORT,
    DATA,
    EASTERN,
    FEATURE_REGISTRY_FILE,
    GATEWAY,
    JOBS_FILE,
    LOG_FILE,
    ORDERS_FILE,
    OUTPUTS,
    PID_FILE,
    PORTFOLIO_HISTORY_FILE,
    ROOT,
    SPORTS,
    _cached,
    _config_payload,
    _count_lines,
    _inject_dashboard_token,
    _log,
    _manual_research_eligibility,
    _number,
    _read_json,
    _resolve_runner,
    _row_has_banned_team,
    _runner_env,
    _runtime_paths,
    _runtime_paths_cache,
    _set_unit_value_usd,
    _today,
    _unit_value_usd,
    rebuild_view,
)
from .data_service import handle as data_service_handle  # noqa: F401 -- re-export for compat
from .evidence import (  # noqa: F401 -- re-export for compat
    _artifact_evidence,
    _artifact_hash,
    _backfill_aliases,
    _deduplicate_ledger_rows,
    _feature_attribution,
    _feature_registry_evidence,
    _ledger_deduplication_key,
    _ledger_evidence_for_source,
    _locked_backfill_evidence,
    _model_evidence_from_rows,
    _model_owns_row,
    _normalized_line,
    _pnl_evidence,
    _production_canary_status,
    _production_model_spec,
    _read_evidence_ledger,
    _read_model_ledger_rows,
    _rolling_declared_hash,
    _version_ledger_evidence,
    model_ledger_comparison,
    production_evidence,
    record_model_ledger_decision,
)
from .jobs import (  # noqa: F401 -- re-export for compat
    _REBUILD_VIEWS,
    _hydrate_jobs,
    _job_status_for_returncode,
    _latest_persisted_action,
    _load_persisted_jobs,
    _persist_jobs,
    job_status,
    start_action,
)
from .matrix import (  # noqa: F401 -- re-export for compat
    _config_production_artifact_path,
    _ml_cell,
    _newest_validation,
    _production_artifact,
    matrix,
)
from .orders import (  # noqa: F401 -- re-export for compat
    InvalidSportError,
    _action_command,
    _activity_link,
    _activity_on_or_after,
    _activity_outcome_side,
    _all_ledger_rows_for_price_scan,
    _amount_value,
    _audit_tail,
    _auto_adjust_unit_value,
    _dashboard_order_status,
    _decode_command_output,
    _decorate_pick,
    _dedupe_picks,
    _event_already_started,
    _filled_entry_for_pick,
    _human_market_name,
    _latest_order_for_pick,
    _live_bbo,
    _live_model_links,
    _load_archive,
    _load_orders,
    _load_portfolio_history,
    _model_version_rank,
    _net_position_quantity,
    _normalize_live_activity,
    _order_readiness,
    _pick_identity,
    _portfolio_history_summary,
    _public_market_question,
    _reconcile_orders,
    _reconcile_orders_locked,
    _safe_sport,
    _save_archive,
    _save_orders,
    _save_portfolio_history,
    _selected_short_pnl,
    _suggested_units,
    _team_name_index,
    archive_action,
    bets_view,
    dashboard_picks,
    dedupe_ledger,
    history_picks,
    live_gateway_slate,
    live_portfolio_view,
    open_picks,
    preview_order,
    preview_position_sell,
    submit_order,
    submit_position_sell,
    today_picks,
)
from .picks import (  # noqa: F401 -- re-export for compat
    _find_pick_by_id,
    _flat_ledger_paths,
    _main_ledger_paths,
    _parse_picks,
    _parse_research_picks,
    _performance_breakdown,
    _performance_game_key,
    _pick_is_scored,
    _pick_pnl,
    _pick_probability,
    _read_split_picks,
    _research_ledger_paths,
    performance,
    performance_for_sport,
    read_flat_picks,
    read_picks,
)
from .routes import (  # noqa: F401 -- re-export for compat
    Handler,
)
from .status import (  # noqa: F401 -- re-export for compat
    _daily_pipeline_status,
    _data_inventory,
    status,
)
